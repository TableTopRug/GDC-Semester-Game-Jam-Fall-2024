using Godot;


public static class TileSetUtils 
{
    //  public static ImageTexture GetImageTextureFromTileSource(TileSet ts, int sourceId, int tileId) 
    //  {
	// 	int tileSrc = this.GetCellSourceId(pos);
			// TileSetAtlasSource tsas = (TileSetAtlasSource)this.TileSet.GetSource(tileSrc);
			// Vector2I atlasCorrds = this.GetCellAtlasCoords(pos);
			// Image img = tsas.Texture.GetImage();
    //  }
}